#系统级别的配置
spring:
  cloud:
    config:
#      http://localhost:3344/master/config-dept-dev.yaml
      uri: http://localhost:3344
      name: config-dept #需要从git上读取的资源名称,不要后缀
      label: master
      profile: dev
